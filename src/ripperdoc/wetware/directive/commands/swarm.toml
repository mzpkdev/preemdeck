description = "Adopt the swarm strategy directive for this session — loads skills/swarm/directive.md into context and applies it to your behavior, then echoes a one-line effect summary; persists nothing."
prompt = """
Load the directive into your own context by running: cat "$HOME/.preemdeck/src/ripperdoc/wetware/directive/skills/swarm/directive.md". Adopt it as your operating directive for the rest of this session and apply it to how you work from here on. Do not dump the directive text back to the user, and do not write preemdeck.json — this is an in-session application only. To set the persisted directive instead, use /directive:set-default. Then echo this summary back as Markdown, verbatim and nothing else: the heading "# Strategy: Swarm" on its own line, then "Runs the deck, doesn't do the work by hand: spins up one-and-done agents (a `runner` to recon, a `critic` to verify, a `fixer` to build), holds a thin synthesis layer, keeps its context clean. The `fixer` edits; every commit is the orchestrator's."
"""
