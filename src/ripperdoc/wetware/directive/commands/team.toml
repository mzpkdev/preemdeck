description = "Show the team strategy directive — prints skills/team/directive.md."
prompt = """
Run the bundled reader — `"$HOME/.preemdeck/preemdeck-runtime" "$HOME/.preemdeck/src/ripperdoc/wetware/directive/scripts/show-mode.ts" team` — and print its output verbatim. This only displays the directive — it writes nothing. To set a directive, use /directive:default.
"""
