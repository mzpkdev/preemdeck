description = "Adopt the team strategy directive for this session — loads skills/team/directive.md into context and applies it to your behavior, then echoes a one-line effect summary; persists nothing."
prompt = """
Load the directive into your own context by running: cat "$HOME/.preemdeck/src/ripperdoc/wetware/directive/skills/team/directive.md". Adopt it as your operating directive for the rest of this session and apply it to how you work from here on. Do not dump the directive text back to the user, and do not write preemdeck.json — this is an in-session application only. To set the persisted directive instead, use /directive:set-default. Then echo this summary back as Markdown, verbatim and nothing else: the heading "# Strategy: Team" on its own line, then "Convenes a crew of specialists (builder, critic, arbiter) and lets them spar: every claim draws a challenger, every challenge carries proof, nothing passes unproven. The chair holds the gavel and lands it."
"""
