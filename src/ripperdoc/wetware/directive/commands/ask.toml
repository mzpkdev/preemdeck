description = "Adopt the ask discretion directive for this session — loads skills/ask/directive.md into context and applies it to your behavior, then echoes a one-line effect summary; persists nothing."
prompt = """
Load the directive into your own context by running: cat "$HOME/.preemdeck/src/ripperdoc/wetware/directive/skills/ask/directive.md". Adopt it as your operating directive for the rest of this session and apply it to how you work from here on. Do not dump the directive text back to the user, and do not write preemdeck.json — this is an in-session application only. To set the persisted directive instead, use /directive:set-default. Then echo this summary back as Markdown, verbatim and nothing else: the heading "# Discretion: Ask" on its own line, then "Traces the forks that actually matter before building, takes the reversible calls solo, routes the big, hard-to-undo ones back as one panel. The premise gets pinged first."
"""
