*.bak

# Environment / secrets
.env
.env.local
.env.*.local

# Logs
*.log

# Host CLIs (local-only overrides)
.claude/settings.local.json

# preemdeck install manifest (generated per-install local state)
.install-manifest.json

# preemdeck shipped Bun runtime (fetched by boot.sh into ~/.preemdeck/.runtime)
.runtime/

# JS/TS tooling (Bun)
node_modules/

# Local dev clone of cmdore for `bun link` (nested .git + its build output)
/vendor/

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# OS — macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd
.TemporaryItems

# OS — Linux
.directory
.Trash-*
.nfs*
.fuse_hidden*
