description = "Set the ask discretion directive (discretion=ask) by writing preemdeck.json."
prompt = """
Run the plugin's scripts/set_mode.py with `discretion ask` as the two positional arguments. The script self-locates preemdeck.json and is the only thing that should touch it. Relay its output verbatim; on a non-zero exit, pass through the valid values it prints. The directive takes effect on the next prompt, when the inject hook reads it.
"""
