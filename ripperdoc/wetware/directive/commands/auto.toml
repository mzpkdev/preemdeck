description = "Show the auto discretion directive — prints skills/auto/directive.md."
prompt = """
Run the plugin's scripts/show_mode.py with `auto` as the positional argument and print its output verbatim. This only displays the directive — it writes nothing. To set a directive, use /directive:default.
"""
