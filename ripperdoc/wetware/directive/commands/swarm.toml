description = "Show the swarm strategy directive — prints skills/swarm/directive.md."
prompt = """
Run the plugin's scripts/show_mode.py with `swarm` as the positional argument and print its output verbatim. This only displays the directive — it writes nothing. To set a directive, use /directive:default.
"""
