{
  "name": "directive",
  "version": "0.1.0",
  "description": "Swappable behavioral directive. A /directive command picks which directive a per-prompt hook injects as context.",
  "hooks": {
    "BeforeAgent": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "${extensionPath}/scripts/inject_mode.py --event BeforeAgent",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
