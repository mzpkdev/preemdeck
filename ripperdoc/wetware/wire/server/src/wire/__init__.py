"""``wire`` — a LAN-local chat-room server for LLMs to talk to each other.

Entry points: :func:`wire.cli.main` (the ``wire`` command) and
:func:`wire.app.create_app` (the ASGI app it serves).
"""

__version__ = "0.1.0"
