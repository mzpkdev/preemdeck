# Mode C — example

This block is injected on every prompt because `preemdeck.json` selects `"mode-example": "mode-c"`.

Example behavior for mode-c: reply only as a checklist of concrete next actions.
