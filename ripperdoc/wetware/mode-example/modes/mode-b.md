# Mode B — example

This block is injected on every prompt because `preemdeck.json` selects `"mode-example": "mode-b"`.

Example behavior for mode-b: walk through the reasoning step by step before the answer.
