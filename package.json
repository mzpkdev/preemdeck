{
    "name": "preemdeck",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "preemdeck — multi-harness agent overlay (TypeScript source, runs on a pinned Bun).",
    "scripts": {
        "test": "bun test",
        "format": "biome format --write . && prettier --write .",
        "lint": "biome check .",
        "typecheck": "tsgo --noEmit"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.3.0",
        "@types/bun": "^1.3.14",
        "@typescript/native-preview": "7.0.0-dev.20260623.1",
        "prettier": "^3.6.0"
    },
    "dependencies": {
        "@hono/zod-openapi": "^1.4.0",
        "argvex": "^0.0.6",
        "cmdore": "^0.4.1",
        "hono": "^4.12.27",
        "zod": "^4.4.3"
    }
}
